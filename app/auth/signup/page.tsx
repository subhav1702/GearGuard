import SignupClient from "./signup-client";

export const metadata = {
  title: "Create Account | GearGuard",
  description: "Join GearGuard to optimize your equipment maintenance lifecycle.",
};

export default function SignupPage() {
  return <SignupClient />;
}
